//DOM-document object 
//when an html code is parsed by the browser it is converted to DOM
//the DOM abstracts the structure of the document into a tree of objects, allowing scripts to manipulate the contents and structure dyanmically
//this abstraction enables more complex iterations and functionalities beyond just static HTML
